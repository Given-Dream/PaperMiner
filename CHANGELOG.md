# PaperMiner 更新日志

## v1.4.24 (2026-09-05)

- 主界面的八个“提取内容”复选框同时驱动 MinerU raw 策略，不再只控制第二阶段 `extract`；隔离 worker 通过显式 `--features` 参数接收每批次已冻结的选择，闪退恢复仍复用同一组设置。
- 未勾选公式或表格时分别关闭 MinerU 的公式识别和表格识别；VLM / hybrid 后端未勾选图片时关闭图片语义分析。默认 pipeline 仍执行联合版面分析，因此可能留下必要图片中间文件，但不再把它误报为已选图片输出。
- raw 只落盘当前提取项需要的文件组合：章节单选只需文本 Markdown；图片会保留 `content list + middle JSON + 原 PDF` 以支持整图重建；标题和代码/数据链接保留原 PDF 作为可审计证据；其他组合按消费者依赖自动取并集。
- 不再生成 PaperMiner 未消费的 MinerU model JSON、layout bbox PDF 和 span bbox PDF；没有图表需求时改用 `nlp_markdown`，避免 Markdown 嵌入未选择的视觉块。
- 主界面明确提示：raw 是所选结果的最小中间集合，今后若要从 raw 补提本次未勾选的内容，必须重新处理原 PDF；worker 日志逐篇列出勾选内容、raw 保留文件及模型开关。

---

## v1.4.23 (开发版)

- 主界面新增“文章标题 / Markdown”提取项，默认启用；每篇论文生成 `Title/文章标题.md`，并写入 `Title/title_scan.json` 审计与完成标记。
- 标题识别不调用 LLM 补写：综合 MinerU 首页标题块/一级文本、Markdown 首部标题和 PDF `/Title` 元数据，再用 PDF 首页文本与源文件名交叉核验；只有文件名可用时明确标记为“需核查”。
- 候选会过滤 `Abstract`、`References`、期刊版权文字、URL/DOI 等非标题内容，并按独立证据的一致程度给出高、中或需核查置信度。
- “合并”入口扩展为文章标题、同名章节、图表、代码/数据地址和参考文献；新增 `MergedSections/文章标题_合并.md`，逐篇保留标题、来源文件和置信度，不跨论文去重。
- 标题选项已纳入批次持久化、闪退恢复、完成签名和“跳过已有结果”判断；合并日志会单列标题总数与需人工核查数。

---

## v1.4.22 (开发版)

- PyTorch 安装策略从“只按驱动版本”升级为“GPU 代际/计算能力 + 驱动 + 真实 CUDA 内核”三重核验，明确区分 RTX 30（Ampere）、RTX 40（Ada）和 RTX 50（Blackwell）。
- RTX 50 / Blackwell 在 Windows 驱动 580.88 及更新版本上使用 `cu130`；570.65–580.87 的兼容区间使用固定的 PyTorch 2.11.0 `cu128` 组合。RTX 30/40 根据驱动使用 `cu126`、`cu121` 或 `cu118`。
- 多卡混合配置按要求最高的显卡选择统一 wheel；安装前后均在每张可见 GPU 上执行 FP32→FP16 张量运算及同步，不再把 `torch.cuda.is_available()` 当作充分条件。
- 已安装 wheel 与目标运行时不符、缺少当前 GPU kernel image 或真实运算失败时，Setup 会输出显卡代际、驱动、现有 PyTorch/CUDA 和目标方案，卸载旧 `torch/torchvision/torchaudio` 后重装匹配组合。
- 显卡存在但驱动低于最低安全线时，不再保留不可执行的 CUDA wheel；Setup 会说明所需驱动版本并换成 CPU wheel，更新驱动后可运行“重装”切回 GPU。

---

## v1.4.21 (开发版)

- 主界面的合并入口扩展为“合并同名章节、图表、代码/数据地址和参考文献”。
- 新增 `MergedSections/参考文献_合并.md`：按论文分组汇总各自的 `References/参考文献.md`，保留每篇论文内部的原文顺序和原编号，不跨论文去重或改写。
- 合并时读取 `references_scan.json`；明确为零条、报告未写入或标记损坏的结果不会纳入汇总，防止旧版残留文件造成误合并。无标记的旧报告仅在能识别到编号条目时兼容纳入。
- 长路径保护生成的短目录会从 `.paperminer-source.json` 恢复完整论文标题，因此章节、图表、代码/数据和参考文献汇总中仍显示原论文名。
- 主界面日志和完成提示新增参考文献来源论文数与累计条目数，便于人工核查。

---

## v1.4.20 (开发版)

- 修复长论文标题在 MinerU 写入 `raw/<标题>/auto/images/<64位哈希>.jpg` 时触发 Windows 传统 260 字符路径边界并报 `FileNotFoundError` 的问题。
- MinerU 处理前按预计最深图片路径进行检查；必要时改用“可读前缀 + 稳定哈希”的短内部目录，并用 `.paperminer-source.json` 保存原论文名和来源路径。
- 短目录映射纳入并行 GPU 精确结果定位、闪退恢复、半成品隔离、仅提取模式和完成标记判断，避免并发时误取另一个任务的最近输出。
- 最终 `extract` 目录在安全时继续保留完整论文名；正文和公式文件名若仍会超过安全预算，则自动改为 `全文.md`、`公式.md`。
- 输出根目录本身过深且连哈希短名也无法容纳时，在模型运行前停止并明确提示改用 `D:\\PaperMinerOutput` 等短输出根，不再等到图片写入阶段失败。

---

## v1.4.19 (开发版)

- 新增“文末参考文献（Markdown）”提取项，每篇论文输出到 `References/参考文献.md`。
- 优先使用 MinerU `*_content_list.json` 的 `ref_text` 结构化条目，保留跨页顺序、原始编号以及不同编号下的重复引用。
- `ref_text` 缺失时，按 `References` / `Bibliography` / `参考文献` 标题边界解析；标题缺失时仅接受文末编号连续、书目信号充分的保守兜底结果。
- 参考文献不经过 LLM 生成或改写，避免作者、年份、题名、页码和 DOI 失真；只归一化 OCR 断行与多余空白。
- 新增 `References/references_scan.json` 完成标记，并将该选项纳入完成签名、跳过判断及闪退恢复配置。

---

## v1.4.18 (2026-08-30)

- 调整中断半成品生命周期：批次暂停或异常中断时继续保留 `Recovery\Interrupted`，以便重启恢复和人工核查。
- 当本批次全部文献进入完成、失败或跳过终态后，自动删除仅属于该批次的中断半成品目录。
- 清理前校验批次 ID、输出根边界、目录前缀与重解析点；不删除当前 `raw` / `extract` 正式结果，不触及其他批次的恢复目录。
- 清理失败时保留对应目录并写入日志，批次记为 `cleanup_pending` 并在下次启动重试，不会扩大删除范围。

---

## v1.4.17 (2026-08-30)

- 新增 SQLite WAL 持久化批次日志，每篇 PDF 在 `pending / parsing / raw_validated / extracting / complete` 阶段更新恢复点；状态保存在 `%LOCALAPPDATA%\PaperMiner\recovery\runs.db`。
- 每篇文献完成后原子写入 `extract/<论文>/.paperminer-complete.json`；只有来源文件指纹和输出选项仍匹配时才视为已完成，避免将闪退留下的半成品误判为成功。
- `PaperMiner.exe` 监护到 GUI 异常退出后，先通过 Windows 作业对象回收遗留 MinerU 进程，2 秒后自动重启并继续未完成批次。
- 崩溃恢复以 PDF 为安全边界：已完成的不重跑，当时正在处理的那篇从头重跑；中断输出移到 `Recovery\Interrupted` 保留，不会删除。
- 正常点击“停止”或关闭会暂停批次并回收 MinerU，下次启动可人工确认继续；不会把人工中止的当前 PDF 误记为永久失败。
- 新增崩溃循环保护：10 分钟内连续异常退出 3 次后停止自动重启，保留批次现场并提示核查 `%LOCALAPPDATA%\PaperMiner\logs\PaperMiner-launcher-error.log`。

---

## v1.4.16 (2026-08-30)

- 修复完成一轮后再次运行时，任务看板仍显示上一轮“处理完成”、100% 进度和旧文件提示的问题；新一轮后台预检开始前会同步重置状态、进度和成功/失败/跳过统计。
- 完整 GPU 批处理期间每 2 秒读取一次 `nvidia-smi` 的逐卡利用率及显存占用，并结合每张卡的任务成功、失败、CUDA OOM 和原生崩溃记录生成下一轮建议。
- 新增任务看板“GPU 智能推荐”：显示逐卡平均利用率、显存峰值和建议任务数，可由用户确认后应用到下一轮，不会在当前任务运行中自动改变并发。
- 推荐采用保守限幅：每张卡每轮最多增减 1 个任务；采样不完整、人工停止或存在解析失败时不提高并发，OOM、原生崩溃或显存接近上限时降低一档。
- GPU 推荐摘要持久化到用户设置；关闭再打开软件后仍可查看上次建议，并会在显卡重新检测完成后允许应用。

---

## v1.4.15 (2026-08-29)

- Anaconda 来源增加官方 `repo.anaconda.com`，与清华、北外、南大镜像一起读取 1 MiB 短样本测速，按实测速度排序；下载源失败或持续低于阈值时自动切换。
- 新增普通 PyPI 五源并行测速：清华、阿里云、中科大、腾讯与 PyPI 官方。MinerU 和常规依赖使用最快的单一索引，镜像缺包或未同步时回退官方 PyPI。
- PyTorch CUDA wheel 继续从 PyTorch 官方索引获取，并允许通用依赖从测速选出的 PyPI 源下载，避免全部小 wheel 固定经过慢速链路。
- GPU 检测日志新增全部 NVIDIA 显卡的索引、型号、显存和驱动；修复 GPU 名称回显命令产生“找不到文件”的引号错误。
- 安装后的验证显示 PyTorch 版本、CUDA runtime、cuDNN 版本、CUDA 设备数和每张设备名称。
- 明确不为二进制 PyTorch 单独安装 CUDA Toolkit/cuDNN，也不静默安装系统显卡驱动；驱动未就绪时使用 CPU 版并提供 NVIDIA 官方驱动入口。

---

## v1.4.14 (2026-08-29)

- Setup 快速自动检查未从明确路径或 PaperMiner 已保存配置找到 Conda 后，立即允许勾选“我确认本机没有 Conda”。
- “检测此目录”和“全盘检索 Conda”改为可选核查，不再是进入多镜像 Anaconda 下载分支的前置条件。
- 用户修改备用 Anaconda 安装目标时保留无 Conda 确认状态，避免重复操作；一旦检测到有效 Conda，仍自动取消并禁用确认框。
- 安装器底部按钮调整为“安装”在左、“取消”在右，符合主要操作优先的常规布局；Enter 与 Esc 行为保持不变。

---

## v1.4.13 (2026-08-28)

- Setup 自动检测采用严格优先级：先使用明确传入的 Conda 路径，再读取 PaperMiner 已保存的运行环境配置和每用户 Conda 路径提示。
- 自动阶段不再隐式扫描磁盘；没有结果时，用户可选择“检测此目录”或主动执行“全盘检索 Conda”。
- 新增后台只读目录树检索，覆盖已就绪的本地固定磁盘和可移动磁盘，显示当前检索路径，并跳过重解析点、Windows、系统卷、回收站、`.git` 和 `node_modules`。
- 指定目录和全盘检索发现候选后实际运行 `conda --version`；只有检索成功且没有找到 Conda，才允许进入多镜像 Anaconda 下载分支。
- 安装分支明确区分“复用已验证 Conda”和“用户确认无 Conda 后安装到指定目录”，避免后续依赖阶段重新选中其他隐式 PATH 环境。

---

## v1.4.12 (2026-08-28)

- 修复 Setup 在第一次安装指定了自定义 Anaconda/Miniconda 目录后，下一次运行无法自动找回该目录的问题；每用户保存 Conda 根目录提示，安装失败后也会保留。
- 自动检测补充各磁盘 `Program Files\Anaconda3`、`Miniconda3` 等常见路径，并支持从用户选定的父目录检查标准子目录。
- 自动检测不到时不再直接下载：Setup 先要求用户指定目录并点击“检测此目录”；只有勾选确认本机没有 Conda 后，才启用多镜像 Anaconda 下载。
- 修复新安装 Anaconda 后创建 MinerU 环境时的 `CondaToSNonInteractiveError`；创建环境使用 `conda-forge --override-channels`，避免依赖 Anaconda 默认频道服务条款状态。
- 安装日志记录复用现有 Conda、人工复检和镜像下载分支，便于在另一台电脑定位问题。

---

## v1.4.11 (2026-08-26)

- `PaperMiner.exe` 增加当前 Windows 会话全局的单实例互斥锁；双击、连续点击、重复启动或从另一份安装目录启动都只保留首个 GUI 进程。
- 启动器改为在后台监护 GUI 全生命周期，并用 `KILL_ON_JOB_CLOSE` Windows 作业对象约束 GUI 及其后代进程。
- 关闭主界面时先阻止新 MinerU 任务启动，再按已登记 PID 终止整个 MinerU 进程树、等待退出，超时后强制回收。
- “停止”与多 GPU 致命错误路径复用同一套进程树清理逻辑；日志记录请求终止数和确认退出数。

---

## v1.4.10 (2026-08-26)

- Setup 打开后先在后台调用与安装阶段相同的 Conda 检测逻辑，检测结束前禁用“安装”按钮。
- 检测到现有 Conda 时，界面显示将复用的真实根目录，并隐藏 Anaconda 安装位置输入框。
- 只有确认未检测到 Conda 或检测失败时，才显示 Anaconda 目录与浏览按钮；该目录也可用于手动指向自动检测遗漏的现有 Conda。
- 新增“重新检测”按钮，便于用户在安装器打开后调整 PATH 或准备好 Conda 再次核查。

---

## v1.4.9 (2026-08-26)

- 新电脑不再要求预装 Conda；未检测到 Conda 时，Setup 从清华、北外、南大中国镜像依次下载 Anaconda 2026.07-1，并以当前用户模式静默安装。
- Setup 新增由用户指定的 Anaconda 安装目录；PaperMiner 与 Anaconda 路径必须分离、不能使用磁盘根目录，也不能包含批处理不安全的 `!`、`%` 字符。
- 安装器锁定官方 SHA-256 与文件大小，校验失败绝不执行；失败镜像自动切换，验证通过的安装包保留用于重试。
- 修复 Conda 已安装在 F 盘等非标准路径、但尚未创建 MinerU 环境时，外层检测成功却没有把 Conda 命令交给依赖脚本的问题。
- 新环境使用 `conda info --json` 返回的真实 `envs_dirs` 和 `--prefix` 创建，避免路径含空格时解析 `conda env list` 文本列失败。
- Conda 检测补充 Windows 卸载登记及 `anaconda` / `miniconda` 常见目录别名；若自动检测仍遗漏，也可在 Setup 的 Anaconda 目录框中直接选择现有安装根目录。
- Setup 日志新增镜像、下载百分比、SHA-256、用户选择目录、静默安装退出码和耗时。

---

## v1.4.8 (2026-08-25)

- 将“文末开源代码地址”升级为“代码与数据可用性”，同时提取代码仓库和研究数据集超链接。
- 扩展 Zenodo、OSF、Figshare、Dataverse、Mendeley Data、Dryad、Kaggle、Hugging Face 等平台，并支持带明确可用性语义的其他网站。
- 新增原 PDF 文末外部超链接注释解析，可恢复 MinerU 文本中被隐藏、HTML 标签拆分或换行截断的真实 URI；长论文扫描范围扩展到最后最多 20 页。
- 明确排除 `doi.org`、出版商 DOI 端点及 Elsevier `refhub/linkinghub` 参考文献跳转地址，不再把引用地址作为资源地址输出。
- 新增仓库 URL 截断消歧：`PINN-` + `FGM`、`phase-field-fracture with-pidl` 等文本碎片会优先归并为 PDF 注释中的完整链接。
- 配置 LLM 后，以单篇一次结构化请求核验候选类型；请求只含既有候选 URL 与局部上下文，模型返回的未知编号或虚构地址不会进入结果。调用失败自动保留本地规则结果。
- 未找到可信地址时不创建 Markdown，仅写 `OpenSource/availability_scan.json` 完成标记；有结果时写 `OpenSource/代码与数据可用性.md`。
- 汇总文件升级为 `MergedSections/代码与数据可用性_合并.md`，只纳入含可信地址的报告；v2 零结果标记会屏蔽旧版空审计文件但不会删除用户文件。
- 新增 `pypdf>=5,<7` 运行依赖及安装/启动校验。

---

## v1.4.7 (2026-08-25)

- 新增后台物理 CUDA 显卡探测，可识别多张 NVIDIA GPU，同时避免显卡初始化阻塞主界面。
- 新增“GPU 并行设置”窗口：可逐卡启用、设置每卡 1–4 个任务槽，并提供全部 GPU 每卡 1 个任务及单卡安全预设。
- GPU 选择和每卡任务数保存到 `%LOCALAPPDATA%\PaperMiner\settings.json`；原有单卡 GPU、CPU 和仅提取 raw 模式继续可用。
- MinerU 子进程通过 `CUDA_VISIBLE_DEVICES` 固定绑定到指定物理显卡，避免多个任务意外集中到 GPU 0。
- 批处理改为 GPU 解析与 CPU/LLM 后处理重叠的流水线；日志增加 `[GPU N][槽 M]` 标签，并由协调线程统一维护进度和成功/失败/跳过统计。
- 停止操作会统一终止全部活动 MinerU 子进程；普通单篇失败继续处理剩余文献，运行环境级致命错误会取消尚未开始的任务。
- 默认采用每张启用显卡 1 个任务的保守配置；用户可在观察显存峰值后逐卡提高并发。

---

## v1.4.6 (2026-08-24)

- 新增“文末开源代码地址”选项：从论文文末正文与 MinerU 文末页面识别 GitHub、GitLab、Gitee、Bitbucket、Zenodo 等代码仓库或软件归档地址。
- 每篇论文都会生成 `OpenSource/开源代码地址.md`，同时保留识别来源、上下文和置信度，便于人工核验；未识别到地址时也会保存核查报告。
- 参考文献区域默认排除，普通归档或 DOI 地址还必须带有代码可用性语义，以减少把引用链接误识别为论文源码。
- “合并同名章节和图表”扩展为“合并同名章节、图表和开源代码地址”，新增 `MergedSections/开源代码地址_合并.md`。
- 勾选新功能后，旧版已经处理但尚无开源代码报告的论文会自动补提取，不会被“跳过已处理结果”误跳过。

---

## v1.4.5 (2026-08-23)

- 输入 PDF 目录和输出根目录改为主界面可选择，不再固定为安装目录下的 `input` / `output`。
- 用户选择保存在 `%LOCALAPPDATA%\PaperMiner\settings.json`，升级程序后继续生效；`raw`、`extract` 与 LLM 调试输出统一跟随所选输出根目录。
- 修复批处理线程直接访问 Tk 变量和调用 Tk 定时器的跨线程风险，所有日志与状态更新统一由主线程队列执行。
- 每篇文献处理后回收 Python 临时对象与 CUDA 缓存，并在日志记录工作集、私有内存和显存变化，降低长批次累积占用导致的强退风险。
- 启用 Python `faulthandler`，原生崩溃时尽可能把所有线程堆栈写入同一份 `logs\PaperMiner_*.log`。
- 每篇 PDF 的 MinerU 推理改由独立子进程执行；CUDA、PyTorch、ONNX Runtime 等原生库即使硬崩溃，也不会再带走 PaperMiner 主界面，日志会保留子进程退出码并继续处理其余队列。
- 修复安装器只检查 MinerU 是否存在、导致旧版 2.6.4 被误判为已安装的问题；安装和启动校验现统一要求 `mineru>=3.1.0,<4.0`，旧环境会进入升级流程。

---

## v1.4.4 (2026-08-21)

- 将“合并同名章节到 Markdown”扩展为“合并同名章节和图表到 Markdown”。
- 图表内容统一读取各论文 `Word` 文件夹下的 Markdown，并生成 `MergedSections/图表汇总_合并.md`。
- 合并图表时自动把 `../Figure/...`、`../Tables/...` 等链接改写为相对于汇总文件的有效路径。
- 支持图题包含 `[49]` 等嵌套方括号，以及论文目录名包含空格的情况。
- 保留原有同名章节输出文件和 LaTeX 原文，不要求所有论文都同时具有章节与图表。

---

## v1.4.3 (2026-08-21)

- 修复 `PaperMiner.exe` 通过 `pythonw.exe` 无控制台启动时，MinerU 导入 `doclayout_yolo` 报错 `'NoneType' object has no attribute 'encoding'` 的问题。
- 在导入 MinerU 前为缺失的 `sys.stdout` / `sys.stderr` 补建完整 UTF-8 文本流，兼容第三方 logging 初始化。
- 保持主程序无 PowerShell、无控制台窗口的启动体验；GUI 与 UTF-8 文件日志不受影响。

---

## v1.4.2 (2026-08-21)

- 修复依赖安装已成功且退出码为 0，但跨盘 Conda 环境被最终运行时检查误报为失败的问题。
- 运行时探测改为读取 `conda env list --json`，支持 Conda 主程序与 `MinerU` 环境位于不同磁盘或自定义 `envs_dirs`。
- Setup 会记录真实的环境目录与 `python.exe`，随后由 `PaperMiner.exe` 直接使用。
- 重装/卸载对 Conda 已注册的精确 `MinerU` 前缀执行操作，并保留名称、根路径与注册状态三重安全检查。
- 明确支持将程序安装到任意可写的非 C 盘子目录，继续拒绝磁盘根目录作为安装目标。
- 修正通过 PATH 找到 Conda 时生成含 `..` 的路径，并兼容 `conda env list` 当前环境带 `*` 标记的输出。
- 安装结束前新增完整运行时依赖复核；Conda、权限或依赖失败现在会向外层 Setup 返回真实非零状态。
- 简化 `cmd.exe` 调用引号，移除日志中的“文件名、目录名或卷标语法不正确”伪错误。

---

## v1.4.1 (2026-08-21)

- 自定义接口模型选择由需要 Ctrl/Shift 的扩展多选列表改为可滚动的单选模型列表。
- 普通单击任一模型即可替换当前选择；测试与保存只处理这一项。
- 移除重复的“默认模型”下拉框，统一显示“当前选择”。
- 修正模型列表区域被窗口布局压缩而看不到模型名称的问题。

---

## v1.4.0 (2026-08-21)

聚焦：现代化横向工作台与软件内实时日志。

### 界面与交互

- 引入 `ttkbootstrap 2.x`，重做按钮、输入框、单选/复选控件、开关、进度条、卡片和设置窗口样式。
- 主界面改为配置、任务、实时日志三栏横向看板；分隔线可拖动调整，1000×720 窄屏仍保持日志可见。
- 日志改为右栏常驻深色控制台，同时继续按 UTF-8 写入 `logs\PaperMiner_*.log`。
- Setup 内置并自动安装 `ttkbootstrap 2.2.2` wheel，同时约束 `>=2.2.2,<3.0`；直接运行但缺少依赖时会显示明确修复提示。
- 联系邮箱更新为 `2878705044@qq.com`。

### 版本

- 版本号升级到 1.4.0。

---

## v1.3.0 (2026-04-21)

聚焦：Word 图表文档的质量提升 + GUI 交互优化。

### 新增 / 修复

**Word 图表文档提取**

- 修复：图片标题取错的问题。旧版在全文 grep `Fig. N.` 时会优先命中正文里 `... as shown in Fig. 4.` 之类的引用句，导致图题要么被正文替换，要么被截断成 `Fig. 4.` 空壳。新版优先在图片下方 12 行内查找行首符合 `Fig N. 描述` 的真图题，再回退到全文扫描，并剔除中间引用句。
- 修复：图片标题里的 LaTeX 公式不渲染的问题。旧版 Word 里会原样呈现 `$\mathbf{c}, \sigma(30^{\circ}\mathbf{C})$` 之类的裸 LaTeX 源码；现已与表格标题一致走 `latex_to_unicode()` 处理，变成可读的 Unicode（例如 `c, σ(30 °C)`）。
- 增强：LaTeX → Unicode 转换对以下情况更稳：
  - `^{\circ}` / `^{\prime}` 等"伪上标"正确转为 `°` / `′`，不再被丢失。
  - `\mathsf C`（无花括号单字符形式）展平为裸字符，避免被渲染成数学 Astral 字母（U+1D5A2 等）导致 Word 里显示成方块。
  - `{\bf of}` / `{\it xyz}` 等 switch 形式中纯字母单词识别后保留词间空格（例如 `Tg of P1-P5` 不再被粘成 `TgofP1-P5`）。
  - `° C` → `°C` 自动合并；数字侧 `30 °C` 保留。

**GUI 优化**

- 默认窗口改为内容自适应高度（上限为屏幕高 92%），启动即完整展示全部面板，无需手动拉伸。
- 修复：处理进度区下方"长黑条"问题。日志区现在有 180px 最小高度，窗口缩小时也不会被压扁。
- 重组处理进度卡：统计方格由 4 格（成功/失败/总计/跳过）简化为 3 格；"总计" 信息合并到状态行的 `X / Y` 显示，避免重复；方格去硬边框、降饱和度。
- 标题栏瘦身：高度 60 → 44；"设置"按钮改为白底蓝字，解决原蓝底蓝字对比度不足、几乎不可见的问题。
- 开始 / 停止按钮宽度比例由 1:1 改为 2:1，突出主次（停止按钮大部分时间为 disabled 态）。
- 文件信息卡右侧新增当前 input 路径显示；输出目录卡新增当前 output 根目录显示。

### 其他

- 修复窗口初始尺寸矛盾（`minsize(960, 1200)` 与 `geometry(960, 720)` 冲突）。
- 版本号升到 1.3.0（`scripts/version.py`）。

### 升级方式

直接覆盖 `scripts/` 下的 `batch_pdf_processor_gui.py` 与 `version.py` 即可，不需要重装环境。已有 v1.2.0 的 Conda 环境和模型保持不变。

---

## v1.2.0

- 一键安装脚本智能检测 GPU 驱动版本，自动选择匹配的 PyTorch CUDA 版本（cu126/cu121/cu118），验证失败自动降级为 CPU 版。
- MinerU 3.0+ 支持：首次运行自动下载模型；国内默认 ModelScope 源。
- 增加 GUI 内"设置"面板：直接配置 API Key，无需手动编辑 `.env`。

## v1.1.0

- 章节提取改用正则表达式 + LLM 双引擎策略，综合成功率 ~98%，平均成本 <0.0003 元/篇。
- 支持 Deepseek 作为默认 LLM。

## v1.0.0

- 首次发布：基于 MinerU 的 PDF 批量提取工具，支持文字 / 公式 / 图片 / 表格 / 章节。
